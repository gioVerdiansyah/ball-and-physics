const renderPlayerPosition = () => {
  // const player1Positions = document.getElementById("player1-positions");
  // player1Positions.innerHTML = `$$x=${player1.x.toFixed(2)} \\quad y=${player1.y.toFixed(2)} \\quad r=${player1.r}$$`;

  // const player2Positions = document.getElementById("player2-positions");
  // player2Positions.innerHTML = `$$x=${player2.x.toFixed(2)} \\quad y=${player2.y.toFixed(2)} \\quad r=${player2.r}$$`;

  // const ballPositions = document.getElementById("ball-positions");
  // ballPositions.innerHTML = `
  // <p>$$x=${ball.x.toFixed(2)} \\quad y=${ball.y.toFixed(2)} \\quad r=${ball.r} \\quad m=${ball.m}$$</p>
  // <p>$$vx=${ball.vx.toFixed(2)} \\quad vy=${ball.vy.toFixed(2)} \\quad ax=${ball.ax.toFixed(2)} \\quad ay=${ball.ay.toFixed(2)}$$</p>
  // `;

  // MathJax.typeset([player1Positions, player2Positions, ballPositions]);
}

const renderFormulas = () => {
  renderPlayerPosition();
}